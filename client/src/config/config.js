/*
 * The client configuration file for FARMS 
 * 
 */

var Config = {
    /**
     * The address of the server that this client must connect to
     */
    ServerAddress : "http://localhost:3001",

    /**
     * The address of this client. This address needs to be where this client is hosted.
     */
    ClientAddress : "http://localhost:3000"
}

export default Config